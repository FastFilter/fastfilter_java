#native-library-config-darwin.properties (macOS-specific)
# macOS-specific Native Library Configuration

search.paths=/usr/local/lib:/opt/homebrew/lib:/usr/lib:${HOME}/lib
library.mapping.crypto=libcrypto.3
library.mapping.ssl=libssl.3